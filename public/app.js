const A=document.getElementById('app');let me=null, jobs=[];const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function api(url,opt={}){const r=await fetch(url,opt);const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Something went wrong');return d}
function nav(){return `<nav class="nav"><div class="logo">CareerBridge</div><div class="navlinks"><button class="btn ghost" onclick="home()">Jobs</button>${me?`<button class="btn secondary" onclick="dashboard()">Dashboard</button><div class="profile"><div class="avatar">${esc(me.name[0])}</div><span class="hide-sm">${esc(me.name)}</span></div><button class="btn ghost" onclick="logout()">Logout</button>`:`<button class="btn ghost" onclick="login()">Login</button><button class="btn primary" onclick="register()">Create account</button>`}</div></nav>`}
function shell(content){A.innerHTML=nav()+content+`<footer class="footer">© 2026 CareerBridge · Built for BCA students & fresh graduates</footer>`}
async function home(){shell(`<section class="hero"><h1>Find your first job. Build your future.</h1><p>A modern student-first job portal for internships, fresher roles and early-career opportunities.</p><div class="search"><input id="q" placeholder="Search jobs, companies, skills or locations"><select id="type"><option value="">All job types</option><option>Full-time</option><option>Part-time</option><option>Internship</option><option>Contract</option></select><button class="btn primary" onclick="loadJobs()">Search</button></div></section><main class="container"><div class="sectionhead"><div><h2>Latest opportunities</h2><p class="muted">Curated roles for students and fresh graduates.</p></div></div><div id="jobs" class="grid"><div class="empty">Loading jobs…</div></div></main>`);await loadJobs()}
async function loadJobs(){try{const d=await api('/api/jobs?q='+encodeURIComponent(document.getElementById('q')?.value||'')+'&type='+encodeURIComponent(document.getElementById('type')?.value||''));jobs=d.jobs;document.getElementById('jobs').innerHTML=jobs.length?jobs.map(j=>`<article class="card job"><div class="row"><span class="tag">${esc(j.job_type)}</span><span class="muted">${esc(j.location)}</span></div><h3>${esc(j.title)}</h3><b>${esc(j.company)}</b><p class="muted">${esc(j.description).slice(0,145)}…</p><div class="tags">${esc(j.skills).split(',').filter(Boolean).map(x=>`<span class="tag">${esc(x.trim())}</span>`).join('')}</div><div class="row"><span class="muted">${esc(j.salary||'Salary not disclosed')}</span><button class="btn primary" onclick="viewJob(${j.id})">View & Apply</button></div></article>`).join(''):`<div class="card empty">No matching jobs found.</div>`}catch(e){alert(e.message)}}
function login(){shell(`<div class="form card"><h2>Welcome back</h2><p class="muted">Log in to apply for jobs or manage applications.</p><div id="msg"></div><form onsubmit="doLogin(event)"><div class="field"><label>Email</label><input name="email" type="email" required></div><div class="field"><label>Password</label><input name="password" type="password" required></div><button class="btn primary">Login</button></form><p class="muted">Demo student: student@demo.com / password123<br>Demo employer: employer@demo.com / password123</p></div>`)}
async function doLogin(e){e.preventDefault();const f=new FormData(e.target);try{const d=await api('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});me=d.user;dashboard()}catch(x){document.getElementById('msg').innerHTML=`<div class="notice error">${esc(x.message)}</div>`}}
function register(){shell(`<div class="form card"><h2>Create your account</h2><p class="muted">Join as a student or employer.</p><div id="msg"></div><form onsubmit="doRegister(event)"><div class="field"><label>Full name</label><input name="name" required></div><div class="field"><label>Email</label><input name="email" type="email" required></div><div class="field"><label>Password</label><input name="password" type="password" minlength="6" required></div><div class="field"><label>I am a</label><select name="role"><option value="student">Student / Job seeker</option><option value="employer">Employer / Recruiter</option></select></div><button class="btn primary">Create account</button></form></div>`)}
async function doRegister(e){e.preventDefault();const f=new FormData(e.target);try{await api('/api/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});login()}catch(x){document.getElementById('msg').innerHTML=`<div class="notice error">${esc(x.message)}</div>`}}
function viewJob(id){const j=jobs.find(x=>x.id==id);A.innerHTML=nav()+`<main class="container"><button class="btn ghost" onclick="home()">← Back to jobs</button><article class="card" style="margin-top:15px"><span class="tag">${esc(j.job_type)}</span><h1>${esc(j.title)}</h1><h3>${esc(j.company)}</h3><p class="muted">📍 ${esc(j.location)} &nbsp; · &nbsp; ${esc(j.category)} &nbsp; · &nbsp; ${esc(j.salary||'Salary not disclosed')}</p><hr style="border:0;border-top:1px solid var(--line);margin:24px 0"><h3>Job description</h3><p>${esc(j.description)}</p><h3>Skills</h3><div class="tags">${esc(j.skills).split(',').map(x=>`<span class="tag">${esc(x.trim())}</span>`).join('')}</div>${me?.role==='student'?`<button class="btn primary" onclick="apply(${j.id})">Apply for this job</button>`:me?`<p class="muted">Employer accounts cannot apply. Use a student account to apply.</p>`:`<button class="btn primary" onclick="login()">Login to apply</button>`}</article></main>`}
function apply(id){const j=jobs.find(x=>x.id==id);A.innerHTML=nav()+`<div class="modal"><div class="modalbox"><div class="row"><h2>Apply to ${esc(j.title)}</h2><button class="btn ghost" onclick="viewJob(${id})">✕</button></div><p class="muted">${esc(j.company)}</p><form onsubmit="submitApplication(event,${id})"><div class="field"><label>Resume (PDF, DOC or DOCX, max 5MB)</label><input name="resume" type="file" accept=".pdf,.doc,.docx" required></div><div class="field"><label>Cover letter</label><textarea name="cover_letter" placeholder="Tell the recruiter why you're a good fit…"></textarea></div><div id="msg"></div><button class="btn primary">Submit application</button></form></div></div>`}
async function submitApplication(e,id){e.preventDefault();const f=new FormData(e.target);try{await api('/api/jobs/'+id+'/apply',{method:'POST',body:f});alert('Application submitted successfully!');dashboard()}catch(x){document.getElementById('msg').innerHTML=`<div class="notice error">${esc(x.message)}</div>`}}
async function dashboard(){if(!me){try{me=(await api('/api/me')).user}catch(e){return login()}}if(me.role==='student')studentDash();else employerDash()}
async function studentDash(){const d=await api('/api/applications/mine');shell(`<main class="container"><div class="sectionhead"><div><h1>Student Dashboard</h1><p class="muted">Track every application in one place.</p></div><button class="btn primary" onclick="home()">Browse jobs</button></div><div class="stats"><div class="card stat"><span class="muted">Applications</span><strong>${d.applications.length}</strong></div><div class="card stat"><span class="muted">Shortlisted</span><strong>${d.applications.filter(a=>a.status==='Shortlisted').length}</strong></div><div class="card stat"><span class="muted">Selected</span><strong>${d.applications.filter(a=>a.status==='Selected').length}</strong></div></div><h2 style="margin-top:35px">My applications</h2><div class="tablewrap"><table class="table"><tr><th>Role</th><th>Company</th><th>Status</th><th>Applied</th><th>Resume</th></tr>${d.applications.map(a=>`<tr><td><b>${esc(a.title)}</b><br><span class="muted">${esc(a.location)}</span></td><td>${esc(a.company)}</td><td><span class="badge tag">${esc(a.status)}</span></td><td>${new Date(a.created_at).toLocaleDateString()}</td><td><a href="/api/resume/${encodeURIComponent(a.resume)}" target="_blank">View</a></td></tr>`).join('')}</table></div></main>`)}
async function employerDash(){const d=await api('/api/employer/applications');const mine=await api('/api/jobs');const own=me.role==='admin'?mine.jobs:mine.jobs.filter(j=>j.employer_id===me.id);shell(`<main class="container"><div class="sectionhead"><div><h1>${me.role==='admin'?'Admin':'Employer'} Dashboard</h1><p class="muted">Post roles and manage candidates.</p></div><button class="btn primary" onclick="postJob()">+ Post a job</button></div><div class="stats"><div class="card stat"><span class="muted">Jobs posted</span><strong>${own.length}</strong></div><div class="card stat"><span class="muted">Applications</span><strong>${d.applications.length}</strong></div><div class="card stat"><span class="muted">Shortlisted</span><strong>${d.applications.filter(a=>a.status==='Shortlisted').length}</strong></div></div><h2 style="margin-top:35px">Candidate applications</h2><div class="tablewrap"><table class="table"><tr><th>Candidate</th><th>Role</th><th>Applied</th><th>Status</th><th>Resume</th></tr>${d.applications.map(a=>`<tr><td><b>${esc(a.student_name)}</b><br><span class="muted">${esc(a.student_email)}</span></td><td>${esc(a.title)}</td><td>${new Date(a.created_at).toLocaleDateString()}</td><td><select onchange="updateStatus(${a.id},this.value)">${['Applied','Shortlisted','Interview','Rejected','Selected'].map(s=>`<option ${s===a.status?'selected':''}>${s}</option>`).join('')}</select></td><td><a href="/api/resume/${encodeURIComponent(a.resume)}" target="_blank">View resume</a></td></tr>`).join('')}</table></div><h2 style="margin-top:35px">My job posts</h2><div class="grid">${own.map(j=>`<div class="card"><h3>${esc(j.title)}</h3><p class="muted">${esc(j.company)} · ${esc(j.location)}</p><p>${esc(j.applicants)} applicants</p><button class="btn danger" onclick="deleteJob(${j.id})">Delete</button></div>`).join('')}</div></main>`)}
function postJob(){shell(`<div class="form card"><h2>Post a new job</h2><form onsubmit="createJob(event)"><div class="field"><label>Job title</label><input name="title" placeholder="Frontend Developer" required></div><div class="field"><label>Company</label><input name="company" required></div><div class="field"><label>Location</label><input name="location" placeholder="Remote / Chandigarh" required></div><div class="field"><label>Job type</label><select name="job_type"><option>Full-time</option><option>Part-time</option><option>Internship</option><option>Contract</option></select></div><div class="field"><label>Category</label><input name="category" placeholder="Web Development" required></div><div class="field"><label>Skills</label><input name="skills" placeholder="React, JavaScript, Git"></div><div class="field"><label>Salary</label><input name="salary" placeholder="₹3–5 LPA"></div><div class="field"><label>Description</label><textarea name="description" required></textarea></div><button class="btn primary">Publish job</button></form></div>`)}
async function createJob(e){e.preventDefault();try{await api('/api/jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});dashboard()}catch(x){alert(x.message)}}
async function updateStatus(id,status){try{await api('/api/applications/'+id,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})})}catch(x){alert(x.message)}}async function deleteJob(id){if(confirm('Delete this job and its applications?')){try{await api('/api/jobs/'+id,{method:'DELETE'});dashboard()}catch(x){alert(x.message)}}}async function logout(){await api('/api/logout',{method:'POST'});me=null;home()}api('/api/me').then(d=>{me=d.user;home()}).catch(home);
